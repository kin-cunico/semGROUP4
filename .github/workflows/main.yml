name: Build, Test (with MySQL), and Package
on: push

jobs:
  test:
    runs-on: ubuntu-22.04

    services:
      mysql:
        # Build MySQL from your own Dockerfile (includes world.sql)
        build:
          context: ./semgroup4-db
          dockerfile: Dockerfile
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -psemgroup4 || exit 1"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=15
        env:
          MYSQL_ROOT_PASSWORD: semgroup4
          MYSQL_DATABASE: world

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # Optional: Force rebuild of MySQL to ensure world.sql is re-imported
      - name: Reset Docker cache (force MySQL rebuild)
        run: docker builder prune -af || true

      - name: Wait for MySQL to be healthy
        run: |
          for i in {1..40}; do
            if mysqladmin ping -h 127.0.0.1 -uroot -psemgroup4 --silent; then
              echo "MySQL is up and running"
              break
            fi
            echo "Waiting for MySQL... ($i/40)"
            sleep 2
          done

      - name: Run Unit + Integration tests
        env:
          DB_HOST: 127.0.0.1
        run: mvn -B clean test

      - name: Generate JaCoCo Coverage Report
        run: mvn jacoco:report

      - name: Upload Coverage Report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco

      - name: Package Application
        run: mvn -B -DskipTests package

      - name: Generate CI Summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Java version: $(java -version 2>&1 | head -n 1)" >> $GITHUB_STEP_SUMMARY
          echo "- Tests passed " >> $GITHUB_STEP_SUMMARY
          echo "- Coverage report generated: target/site/jacoco/index.html" >> $GITHUB_STEP_SUMMARY